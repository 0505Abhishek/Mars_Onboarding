<%- include("../shared/super_header.ejs") %>
<style>
    .select2-container {
        width: 100% !important;
    }
    .select2-container--default .select2-selection--multiple .select2-selection__choice__remove{
        width: auto !important;
    }
    .select2-container--default .select2-selection--multiple .select2-selection__clear{
        width: auto !important;
    }
    .select2-container--default .select2-selection--multiple .select2-selection__choice {
        background-color: #0d6efd !important;
        border: 1px solid #0d6efd !important;
    }

    .select2-container--default .select2-selection--multiple {
        background-color: #f8f9fa;
        border: 1px solid #ced4da;
        border-radius: 4px;
        min-height: 38px;
    }

    .select2-container--default .select2-selection--multiple .select2-selection__choice {
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 2px;
        margin-top: 5px;
        padding: 2px 5px;
    }

    .select2-container--default .select2-selection--multiple .select2-selection__choice__remove {
        color: white;
        margin-right: 5px;
    }

    .select2-container--default .select2-selection--multiple .select2-selection__rendered {
        padding: 0 5px;
    }

    .select2-dropdown {
        border-radius: 4px;
    }

    .select2-results__option {
        padding: 6px 12px;
    }

    .select2-results__option--selected {
        background-color: #00b3ff !important;
        color: white !important;
    }
</style>
<div class="main-panel distributors-details">
    <div class="content-wrapper">
        <div class="row">
            <div class="m-3 mt-0">
                <a href="/tab/<%= user.role_id %>" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Back</a>
            </div>
            <div class="col-12 grid-margin stretch-card">
                <div class="card p-3 table-responsive">
                <div class="card-header">
                    Edit User
                </div>
                <div class="card-body">
                    <form id="editUserForm" action="/tab/update" method="POST">
                        <input type="hidden" name="id" value="<%= user.id %>">
                        <input type="hidden" name="role_id" value="<%= user.role_id %>">
                        
                        <div class="form-group mb-3">
                            <label>User Name</label>
                            <input type="text" name="employee_name" class="form-control" value="<%= user.employee_name %>" required>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label>Email</label>
                            <input type="email" name="email_id" class="form-control" value="<%= user.email_id %>" required>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label>Mobile Number</label>
                            <input type="number" 
                            name="mobile_no" 
                            pattern="[1-9][0-9]{9}"
                            oninput="javascript: if (this.value.length > 10) this.value = this.value.slice(0, 10); if(this.value.startsWith('0')) this.value = this.value.substring(1);"
                            class="form-control" 
                            required value="<%= user.mobile_no %>">
                            
                        </div>
                        
                        <div class="row">
                            <div class="col-md-12 form-group">
                                <label>Regions</label>
                                <%
                                  var selectedRegions = (user.region || '').split(',');
                                %>
                                <select name="region" class="select2-dropdown" multiple="multiple"
                                    id="regionsSelect" required>
                                    <option value="select_all">Select All</option>
                                    <option value="remove_all">Remove All</option>
                                    <% regions.forEach(function(region) { %>
                                        <option value="<%= region.id %>"
                                            data-email="<%= region.email_id %>"
                                            data-territory="<%= region.territory %>"
                                            data-region="<%= region.region %>"
                                            data-channel="<%= region.channel %>"
                                            <%= selectedRegions.includes(String(region.id)) ? 'selected' : '' %>>
                                            <%= region.region_name %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-12 form-group">
                                <label>Territories</label>
                                <select name="territories" class="select2-dropdown" multiple="multiple"
                                    id="territoriesSelect" required>
                                   
                                    <option value="select_all">Select All</option>
                                    <option value="remove_all">Remove All</option>
                                    <% territories.forEach(function(territory) { %>
                                        <option value="<%= territory.territory_id %>" selected>
                                            <%= territory.territory_name %>
                                        </option>
                                    <% }); %>
                                </select>
                            </div>
                        </div>
                        
                       
                        
                        <button type="submit" class="btn btn-primary">Update</button>
                        <a href="/tab/<%= user.role_id %>" class="btn btn-secondary">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include("../shared/super_footer.ejs") %>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
    $(document).ready(function () {
        $('.select2-dropdown').select2({
            placeholder: "Select options",
            allowClear: true,
            templateResult: function (state) {
                if (!state.id) {
                    return state.text;
                }
                var $state = $(
                    '<span>' +
                    '<strong>' + state.text + '</strong>' +
                    (state.element.getAttribute('data-email') ?
                        '<br><small class="text-muted">' + state.element.getAttribute('data-email') + '</small>' :
                        ''
                    ) +
                    '</span>'
                );
                return $state;
            },
            templateSelection: function (state) {
                return state.text;
            },
            matcher: function (params, data) {
                if ($.trim(params.term) === '') {
                    return data;
                }
                var searchTerm = params.term.toLowerCase();
                var text = data.text.toLowerCase();
                var email = data.element.getAttribute('data-email') ?
                    data.element.getAttribute('data-email').toLowerCase() : '';

                if (text.indexOf(searchTerm) > -1 || email.indexOf(searchTerm) > -1) {
                    return data;
                }

                return null;
            }
        });

        $('#regionsSelect').on('change', function () {
            var $select = $(this);
            var selectedValues = $select.val() || [];
            if (selectedValues.includes('select_all')) {
                var allRegionValues = $select.find('option')
                    .map(function () {
                        return (this.value !== 'select_all' && this.value !== 'remove_all') ? this.value : null;
                    })
                    .get();

                $select.val(allRegionValues).trigger('change.select2');
            }
            else if (selectedValues.includes('remove_all')) {
                $select.val('').trigger('change.select2');
            }

            fetchTerritories();
        });

        $('#territoriesSelect').on('change', function () {
            var $select = $(this);
            var selectedValues = $select.val() || [];
            if (selectedValues.includes('select_all')) {
                var allTerritoryValues = $select.find('option')
                    .map(function () {
                        return (this.value !== 'select_all' && this.value !== 'remove_all') ? this.value : null;
                    })
                    .get();

                $select.val(allTerritoryValues).trigger('change.select2');
            }
            else if (selectedValues.includes('remove_all')) {
                $select.val('').trigger('change.select2');
            }
        });

        function fetchTerritories() {
            var selectedRegionIds = ($('select[name="region"]').val() || [])
                .filter(function (value) {
                    return value !== 'select_all' && value !== 'remove_all';
                });

            // Store currently selected territories before emptying
            var currentlySelected = $('#territoriesSelect').val() || [];
            
            if (selectedRegionIds && selectedRegionIds.length > 0) {
                $.ajax({
                    url: '/tab/get-territories',
                    method: 'POST',
                    data: { regionIds: selectedRegionIds },
                    success: function (territories) {
                        // Keep the select_all and remove_all options
                        $('#territoriesSelect').empty()
                            .append('<option value="select_all">Select All</option><option value="remove_all">Remove All</option>');

                        if (territories && territories.length > 0) {
                            // Remove duplicates based on territory ID
                            var uniqueTerritories = territories.reduce((unique, territory) => {
                                if (!unique.some(item => item.id === territory.id)) {
                                    unique.push(territory);
                                }
                                return unique;
                            }, []);

                            uniqueTerritories.forEach(function (territory) {
                                // Check if this territory was previously selected
                                var isSelected = currentlySelected.includes(territory.id.toString());
                                $('#territoriesSelect').append(
                                    `<option value="${territory.id}" 
                                        data-region="${territory.region}" 
                                        data-channel="${territory.channel}"
                                        ${isSelected ? 'selected' : ''}>
                                        ${territory.territory_name}
                                    </option>`
                                );
                            });
                        } else {
                            $('#territoriesSelect').append('<option value="">No territories found</option>');
                        }

                        // Re-initialize select2 with the previous selections
                        $('#territoriesSelect').select2({
                            placeholder: "Select territories",
                            allowClear: true
                        });
                    },
                    error: function (xhr, status, error) {
                        console.error('Error fetching territories:', error);
                        $('#territoriesSelect')
                            .empty()
                            .append('<option value="select_all">Select All</option><option value="remove_all">Remove All</option><option value="">Error loading territories</option>')
                            .select2({
                                placeholder: "Select territories",
                                allowClear: true
                            });
                    }
                });
            } else {
                // If no regions selected, just keep the control options
                $('#territoriesSelect')
                    .empty()
                    .append('<option value="select_all">Select All</option><option value="remove_all">Remove All</option>')
                    .select2({
                        placeholder: "Select territories",
                        allowClear: true
                    });
            }
        }

        const backdropDocument = document.getElementsByClassName('modal-backdrop');
        $('#userTransferModal').on('show.bs.modal', function (event) {
            // Hide all backdrop elements
            Array.from(backdropDocument).forEach(backdrop => {
                backdrop.style.display = 'none';
            });
        });

        $('#userTransferModal').on('shown.bs.modal', function (event) {
            // Hide all backdrop elements
            Array.from(backdropDocument).forEach(backdrop => {
                backdrop.style.display = 'none';
            });

            // Reinitialize Select2 inside the modal to ensure proper styling
            $(this).find('.select2-dropdown').select2({
                placeholder: "Select options",
                allowClear: true,
                dropdownParent: $(this)
            });
        });

        $('#userTransferModal').on('hide.bs.modal', function (event) {
            // Remove 'show' class from all backdrop elements
            Array.from(backdropDocument).forEach(backdrop => {
                backdrop.classList.remove('show');
            });
        });

        $('#userTransferModal').on('hidden.bs.modal', function (event) {
            // Remove 'show' class from all backdrop elements
            Array.from(backdropDocument).forEach(backdrop => {
                backdrop.classList.remove('show');
            });

            // Reset the form when modal is closed
            $(this).find('form')[0].reset();

            // Clear Select2 selections
            $(this).find('.select2-dropdown').val(null).trigger('change');
        });

        // Form submission handler
        $('#userTransferForm').on('submit', function (e) {
            e.preventDefault();

            // Validate form
            if (this.checkValidity()) {
                // Collect form data
                var formData = $(this).serialize();

                $.ajax({
                    url: '/tab/update', 
                    method: 'POST',
                    data: formData,
                    success: function (response) {
                      window.location.href = '/tab';
                        // Close modal
                        $('#userTransferModal').modal('hide');
                    },
                    error: function (xhr, status, error) {
                        // Show error message
                        alert('Error submitting user transfer: ' + error);
                    }
                });
            } else {
                // Trigger form validation
                this.classList.add('was-validated');
            }
        });
    });
</script>
